# ClinicaFlow - Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# API Configuration
# Recommended: same-origin API (works with Vite proxy in dev and Nginx proxy in Docker)
VITE_API_URL=/api

# Environment
VITE_NODE_ENV=development
